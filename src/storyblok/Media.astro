---
import { Image } from "astro:assets";

const { blok } = Astro.props;
const filename = blok.asset.filename;
---

{
  filename && (
    <Image
      class="h-full w-full object-contain"
      src={`${filename}/m/1920x0`}
      inferSize
      alt="image"
    />
  )
}
